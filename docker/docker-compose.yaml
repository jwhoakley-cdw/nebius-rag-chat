services:
  web:
    image: jwhoakley/nebius-rag-chat:v1.1
    #image: jwhoakley/nebius-rag-chat:latest
    container_name: rag-chat
    ports:
      - "8501:8501"
    # Environment vars - NEBIUS_API_KEY, TAVILY_API_KEY
    env_file:
      - .env
    restart: unless-stopped

